
const Wrapper = ({children}) => (
    <div className="container-box">
        <section className="col-md-5 border rounded">
            {children}
        </section>
    </div>
)

export default Wrapper